import { Upload } from "@/screens/Upload";

import { constructMetadata } from "@/helpers/constructMetadata";

export const metadata = constructMetadata("Configure your custom phone case");

export default Upload;
